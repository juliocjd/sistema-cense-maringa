# Release Notes

## 2025-11-XX �?- Monitoramento de conflitos e aloca����es automatizadas

- **Risco centralizado:** o motor lib/riscos/calcular agora retorna itens detalhados (tipo do alerta, proximidade e nivel 0-5) e alimenta a rota GET /api/verificar-alocacao, eliminando divergencias entre mapa operacional, sugestoes de destino e a verificacao previa usada no modal de transferencia/alocacao.
- **Modal + Visao Geral:** o modal de detalhes exibe um bloco verde de 'Conflitos resolvidos' com adversario, local e data, e o mesmo componente foi reusado na aba Visao Geral (sem pop-ups do navegador), mantendo as acoes de alocar, transferir, desalocar e interditar em um unico fluxo.

- **Cadastro + alocaÃ§Ã£o automÃ¡tica:** o formulÃ¡rio `/adolescentes/novo` agora oferece seleÃ§Ã£o de alojamento livre, dispara `/api/alocar` automaticamente apÃ³s o `POST /api/adolescentes` (com justificativa padronizada) e redireciona para o dossiÃª, mantendo alertas/justificativas existentes.
- **Painel de agentes e conflitos:** adicionamos `/conflitos` (novo componente `components/conflitos/painel-conflitos.tsx`) que consome `agentes_profissionais`, `bairros_conflitos` e `faccoes_conflitos`, mostra filtros por tipo e alerta os operadores sobre conflitos ativos, com botÃ£o â€œNotificar agentesâ€ acionando o endpoint recÃ©m-criado `POST /api/conflitos/[id]/notificar`.
- **Conflitos externos:** `/inteligencia/conflitos` ganhou layout completo (cards, filtros e botÃµes â€œVer impactosâ€/â€œEncerrarâ€), formulÃ¡rios guiados de bairros e facÃ§Ãµes, alÃ©m do relatÃ³rio exportÃ¡vel baseado no novo endpoint `GET /api/inteligencia/conflitos/impacto`. As listas usam `lib/inteligencia/conflitos.ts` para cruzar adolescentes com territÃ³rios/facÃ§Ãµes em risco.
- **FormulÃ¡rios completos:** criamos telas `/agentes` e `/conflitos` para cadastrar agentes profissionais e conflitos diretamente da UI, com validaÃ§Ãµes e feedbacks visuais, eliminando a necessidade de usar APIs manualmente.
- **NotificaÃ§Ãµes ricas:** o serviÃ§o de notificaÃ§Ã£o (`lib/notificacoes/agente.ts`) agora inclui nÃ­vel de risco e link para o painel e o endpoint `POST /api/conflitos/[id]/notificar` passa esse contexto; a UI exibe o status da notificaÃ§Ã£o para cada conflito.
- **NotificaÃ§Ã£o e backend:** implementamos `lib/notificacoes/agente.ts` (envio de e-mails ou log) e tocamos `app/api/conflitos/[id]/notificar/route.ts` para acionar o serviÃ§o com os adolescentes e agentes vinculados; hooks existentes (`verificar-alocacao`, `grupos/.../adicionar-membro`) jÃ¡ consideram os mapas de conflitos.
- **UI/UX:** sidebar com hover persistente, scroll estilizado e novas docas; documentamos o QA ampliado e criamos `docs/QA-PAINEL-CONFLITOS.md` para orientar testes manuais. TambÃ©m mantemos o backlog em `docs/BACKLOG-FUNCIONALIDADES.md`.
- **MigraÃ§Ã£o e prisma:** adicionamos `20251108195900_add-conflitos-agentes` com novas tabelas/campos (`agentes_profissionais`, `faccoes_conflitos`, campos de ato gravidade/desinternalizaÃ§Ã£o), e totalizamos cÃ³digos de notificaÃ§Ã£o/alocaÃ§Ã£o com logs/auditoria.

- **RelatÃ³rio de impacto sob demanda:** o botÃ£o â€œRelatÃ³rio de impactoâ€ em `/inteligencia/conflitos` agora abre um modal (reutilizando `relatorio-impacto-card`) para gerar/exportar o CSV sem carregar toda a interface principal.
- **Cadastro inline de catÃ¡logos:** a etapa de vinculaÃ§Ãµes do formulÃ¡rio (`/adolescentes/novo`) ganhou os botÃµes `+ Nova facÃ§Ã£o` e `+ Novo bairro`, com modais validados e seleÃ§Ã£o automÃ¡tica do item criado.
- **Mapa operacional inteligente:** o mapa passou a considerar conflitos externos ao pintar alojamentos e oferece um modal de detalhes com aÃ§Ãµes de remover, desinternar, transferir (com verificaÃ§Ã£o), abrir alocaÃ§Ã£o e interditar/desinterditar com justificativa + CI. Veja `docs/QA-MAPA-OPERACIONAL.md`.
- **CorreÃ§Ãµes recentes:** ajustes no modal de detalhes evitaram erros de compilaÃ§Ã£o (hooks duplicados) e mensagens de interdiÃ§Ã£o incorretas quando hÃ¡ ocupantes.
- **Modal detalhado:** o painel do mapa agora lista os adolescentes envolvidos em cada alerta (interno ou externo), exibindo alojamento/casa dos demais participantes e separando os nomes da descricao. O campo de justificativa na aba Transferir/realocar so aparece apos escolher destino e quando a checagem exigir texto, reduzindo ruido.
- **Prioridade das cores:** alojamentos ocupados ignoram `corRisco` manual quando existem alertas ativos; conflitos internos/externos passam a definir a cor (vermelho/amarelo) automaticamente e apenas alojamentos livres exibem o tom configurado manualmente.
- **Escala 0-5 detalhada:** o mapa agora apresenta o "Nivel de risco" (1 a 5) diretamente no modal, diferenciando "Monitorar" (2) e "Elevado" (4) e mantendo a legenda alinhada com a API de verificacao.
  - **UI do mapa operacional:** legenda compacta no topo, cards exibem nome (linha superior) + sobrenome (linha inferior) e a Casa 8 destaca a fase 3 com oito leitos divididos em dois blocos (4 por lado). O badge passou a mostrar â€œAliados do rival na casaâ€ com fundo translÃºcido e tambÃ©m ganhou referÃªncia na legenda.
  - **EdiÃ§Ã£o de adolescentes:** pÃ¡gina `/adolescentes/[id]/editar` reaproveita o formulÃ¡rio completo, prÃ©-preenche dados e envia `PUT /api/adolescentes/[id]`, permitindo revisar alertas, vÃ­nculos e status sem refazer o cadastro.
  - **Cobertura completa no mapa:** `/estrutura` agora busca ate 500 adolescentes por ciclo e reaproveita os IDs de bairro/faccao dos ocupantes, garantindo que rivais na mesma ala ou casa recebam a cor correta automaticamente.
- **TensÃ£o ambiental:** alÃ©m do risco pessoal, o mapa exibe um badge â€œAla em tensÃ£oâ€ quando aliados de rivais ou conflitos indiretos se concentram na mesma casa; o modal detalha os motivos para apoiar decisÃµes operacionais.
- **Sugest?es de aloca??o:** a aba Transferir/realocar agora consulta `/api/alocar/sugestoes` para listar destinos recomendados (n?vel textual e alertas ambientais) e preencher automaticamente casa/alojamento antes da confirma??o.
- **Motor unico de risco:** consolidamos toda a avaliacao (mapa operacional, `/api/casas/status` e `lib/alocacao/sugestoes.ts`) em `lib/riscos/calcular.ts`, garantindo que os niveis 0-5, rotulos e justificativas sejam identicos independente da tela. O endpoint `GET /api/casas/status` agora reaproveita o motor e devolve alertas coerentes, enquanto a API de sugestoes injeta o adolescente no alojamento simulado antes de chamar o mesmo calculo.
- **Mapa alimentado pelo dossie + conflitos externos:** o painel `/estrutura` agora sincroniza cada alojamento com o dossie completo do adolescente (garantindo que todos os conflitos ativos estejam disponiveis ao motor) e consome `/api/inteligencia/conflitos/impacto?status=ATIVO` para pintar automaticamente os riscos territoriais/faccionais, evitando que alojamentos em situacao critica apareÃ§am como nivel 1.
- **Visao Geral alinhada ao mapa (sem alterar layout):** mantivemos a grade tradicional por casa, mas os mesmos modais e verificacoes do mapa operacional sao usados ao clicar em um alojamento, garantindo fluxo padronizado sem mudar o visual original.
- **Regras de proximidade unificadas:** conflitos internos, externos e de aliados agora respeitam a mesma matriz de proximidade (frontal, mesma ala, mesma casa e janelas especÃ­ficas definidas para Casas 02â†”03, 04â†”05, 05â†”06 e 06â†”07). Qualquer rival fora dessas zonas deixa de gerar alerta, e o `/api/verificar-alocacao` passou a aplicar o mesmo helper para manter coerÃªncia com o mapa.
- **Documentacao atualizada:** `docs/QA-MAPA-OPERACIONAL.md` cobre o novo fluxo (risk engine centralizado e relatorio de impacto on-demand) e o presente arquivo registra o passo-a-passo para validar sugestoes, legenda e alertas ambientais.
- **Estabilizacao da Visao Geral:** corrigimos a chamada a `/api/casas/status` (ReferenceError em `slotsPorAdolescente`) substituindo o iterador ausente por `mapaSlots.forEach`, eliminando o erro 500 que interrompia o carregamento da aba.
- **Listagem de conflitos resiliente:** `components/conflitos/listagem-conflitos.tsx` agora ignora registros sem participantes (ou com payload inconsistente), evitando o crash em `conflitosFiltrados.map` quando o array vinha vazio da API.
- **Fallback na UI:** quando o backend retornar conflitos sem participantes, a lista exibe o card com o rÃ³tulo â€œConflito sem participantes cadastradosâ€ em vez de ocultar o item, garantindo visibilidade completa do inventÃ¡rio.
- **Aviso visual amigavel:** o mesmo componente passou a rotular esses casos como "Participantes nao informados" na area de badges, mantendo um titulo mais util (descricao/tipo do conflito) sem poluir a listagem.
- **Normalizacao da listagem de conflitos:** `/conflitos` agora transforma a resposta de `/api/conflitos` em `participantes[]` antes de passar para a UI, assegurando que nomes existentes aparecam corretamente (sem cair no fallback de participantes ausentes).
- **Detalhe do conflito alinhado ao backend:** `/conflitos/[id]` converte o payload de `/api/conflitos/[id]` (campos `tipo`, `dataRegistro`, `participantes`) para o formato esperado pelo `DetalhesConflito`, garantindo que a seÃ§Ã£o â€œParticipantesâ€ volte a ser exibida.
- **Identificacao rapida na Visao Geral:** cada box de alojamento em `/estrutura?tab=visao-geral` exibe o primeiro e ultimo nome do adolescente alojado (com truncamento), sem alterar o tamanho do card, facilitando localizar internos diretamente na grade resumida.
- **Etiqueta de interdicao visivel:** alojamentos interditados na Visao Geral agora exibem explicitamente o selo "Interditado" (em vez de "Livre"), evitando leituras equivocadas na grade compacta.
- **API de validacao estabilizada:** corrigimos `/api/verificar-alocacao` (trecho `if (!alojamentoAlvo)`) para evitar erros de parse que quebravam o build apÃ³s o refactor do motor de risco.
- **Modal de alojamento estateless:** fechar o `ModalAlojamentoDetalhes` agora limpa transferencias/interdicoes pendentes, garantindo que dados de um alojamento nao â€œvazemâ€ quando o operador abre outro.
- **Risco unificado para rivais externos/aliados:** `/api/verificar-alocacao` passou a considerar proximidade fÃ­sica (frontal, mesma ala, mesma casa) tanto para conflitos internos quanto externos e aliados, elevando rivais frontais externos para nÃ­vel 5 e diferenciando aliados (nÃ­vel 4/3/2 conforme distÃ¢ncia).

### Testes
- `npm run test` (vitest run) â€” âœ… todas as suites. O log `500` em `tests/api/analytics-alertas.test.ts` Ã© o cenÃ¡rio previsto.

> Importante: antes de subir para produÃ§Ã£o, execute `npx prisma migrate deploy` e verifique `/conflitos` no browser conforme o checklist em `docs/QA-PAINEL-CONFLITOS.md`.


